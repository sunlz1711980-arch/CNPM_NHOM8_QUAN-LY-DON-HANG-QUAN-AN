def khoi_tao_ban():
    so_ban = int(input("Nhập số lượng bàn: "))
    ban_list.clear()

    for i in range(1, so_ban + 1):
        ban_list.append({
            "ma_ban": i,
            "trang_thai": "Trống",
            "ma_don": None
        })

    print("✅ Đã khởi tạo danh sách bàn!")
